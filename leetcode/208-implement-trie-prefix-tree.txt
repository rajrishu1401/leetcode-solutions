1class Trie {
2public:
3    struct node{
4        node* val[26]={nullptr};
5        bool isEnd=false;
6    };
7    node* start;
8    Trie() {
9        start=new node();
10    }
11    
12    void insert(string word) {
13        node* curr=start;
14        for(char i:word){
15            if(curr->val[i-'a']==nullptr){
16                curr->val[i-'a']=new node();
17            }
18            curr=curr->val[i-'a'];
19        }
20        curr->isEnd=true;
21    }
22    
23    bool search(string word) {
24        node* curr=start;
25        for(char i:word){
26            if(curr->val[i-'a']==nullptr){
27                return false;
28            }
29            curr=curr->val[i-'a'];
30        }
31        if(curr->isEnd){
32            return true;
33        }
34        return false;
35    }
36    
37    bool startsWith(string prefix) {
38        node* curr=start;
39        for(char i:prefix){
40            if(curr->val[i-'a']==nullptr){
41                return false;
42            }
43            curr=curr->val[i-'a'];
44        }
45        return true;
46    }
47};
48
49/**
50 * Your Trie object will be instantiated and called as such:
51 * Trie* obj = new Trie();
52 * obj->insert(word);
53 * bool param_2 = obj->search(word);
54 * bool param_3 = obj->startsWith(prefix);
55 */